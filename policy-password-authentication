---
config:
  layout: fixed
---
flowchart LR
 subgraph SubApps[" "]
        D1["<b>4a. V4 SPA</b><br>Auth0 SPA SDK<br>VITE_AUTH_PROVIDER=auth0<br><br>No local password field or store<br>Inactivity timeout 30 minutes"]
        D2["<b>4b. V4 API</b><br>FastAPI in rw-v4-backend<br><br>Bearer JWT on every request<br>Issuer and audience checked<br>No server-side session"]
  end
 subgraph Stack["V4 Authentication Stack"]
        A["<b>1. Workforce user</b><br>Opens https://app.v4.referwell.com. Identifies with a ReferWell corporate email.<br>Does not type a V4 password."]
        B["<b>2. IDP  — source of identity</b><br>Authenticates the @referwell.com account (password + 2-Step Verification as configured by IT).<br>Directory groups record which ReferWell users are allowed to complete V4 login."]
        C["<b>3. Auth0 — OIDC broker</b><br>Custom domain auth0.v4.referwell.com. Collects the email, redirects to the IDP when no IDP session exists,<br>runs a post-login Action that enforces group membership, then issues a JWT to the SPA."]
        SubApps
  end
    A --> B
    B --> C
    C --> D1
    D1 -- Authorization: Bearer JWT --> D2

     D1:::step
     D2:::step
     A:::step
     B:::step
     C:::step
    classDef step fill:#ffffff,stroke:#2b6cb0,stroke-width:1px,color:#000000
    classDef container fill:#f7fafc,stroke:#4a5568,stroke-width:1px
